# Handoff — Living Project State

Read this first every session. Update it when the user asks, and whenever a ticket closes or a
decision is made that would leave this file materially stale — it should stay current enough that a
fresh session can resume cold from it alone.

## Current phase

**Phase 0 — complete.** Data profiled and documented
([docs/01-data-findings.md](01-data-findings.md)), system designed
([docs/02-system-design.md](02-system-design.md)), and the delivery harness scaffolded
([docs/03-harness-design.md](03-harness-design.md)): `.github/agents/`, `.github/prompts/`,
`.github/instructions/`, `.github/copilot-instructions.md`, `docs/DOD.md`, this file.

**Phase 1 ETL — in progress.**
- **DT-001 (harness bootstrap)**: Complete & approved. Skeleton `packages/wayfinding/`, `infra/docker-compose.yml` test service, `Makefile` gates (`test`/`lint`/`type`), ADR-0002.
- **DT-002 (ETL infrastructure & schema)**: Complete & approved. `etl` service in `infra/docker-compose.yml`, `make etl` target, `category_mapping.yaml` (all 62 `USE_TYPE` values), `schema.py` (7 Pydantic schemas with EPSG:26910 native CRS metadata), `run.py` (entry point). 19 passed, 2 skipped, 96.67% coverage.
- **DT-003 (extract layers to GeoPackage)**: Complete & approved. `extract.py::extract_to_gpkg()` function, `make etl-extract` target, `build/wayfinding.gpkg` with 14 layers (7 EPSG:26910 native 3D, 7 EPSG:4326 web 2D), 158,694 records (79,347 features × 2 CRS). 51 tests passed, 2 skipped, 95.52% coverage. Lint/type clean, data-QA PASS, judge APPROVE.
- **DT-004 (normalise tables)**: Complete & approved. `normalise.py` with 5 normalisation functions transforming raw layers into 6 tables (facility 3, level 11, unit 1017, landmark 40, detail 48728, door 6865; total 56,664 normalised records) stored as 12 dual-CRS layers in `build/wayfinding.gpkg` (26 total layers: 14 raw + 12 normalised). Includes category mapping, in-memory landmark dedupe (LMK_28 kept / LMK_41 removed), accessibility provenance (`verified_by`, `verified_date`), centroid fallback (1016 geometry_centroid + 1 envelope_center_fallback for SFU_BURNABY_QUAD_2000_2017), door separation. ADR-0003 documents dual-CRS storage & provenance model. 105 tests passed, 2 skipped, 92.74% coverage. Lint/type clean, data-QA PASS, judge APPROVE. Commits: adb9196, fbaf84b, 221373d.
- **DT-005 (graph node snapping and raw pathway graph)**: Complete & approved. `graph.py` with `load_level_lookup()`, `snap_nodes()`, `build_raw_graph()` functions. Outputs: `build/graph_raw.pkl` (NetworkX MultiDiGraph, 15,582 nodes, 44,826 directed pathway arcs, 12.4 MB), `build/node_map.pkl` (44,978 entries: 44,852 pathway + 126 transition endpoints, 1.5 MB), `build/graph_raw_stats.json` (metadata). Node identity per ADR-0004 uses coordinate tuples `(x, y, vertical_order)` with 1 cm EPSG:26910 grid snapping and vertical_order from `level_26910` table (never Z). All 11 levels represented. 13 self-loops removed. Pathway edges only (mode='pathway'); transition edges deferred to DT-006. Bidirectional arcs with forward key `PW_{fid}` and reverse key `PW_{fid}_R`. Edge attributes: `{length_3d, mode, level_id, feature_id, geometry}` with reverse-arc geometries coordinate-reversed. Within-level connectivity: largest component 2,957 nodes (AQ 3000), highest fragmentation 253 components (AQ 6000, pre-contraction). ADR-0004 documents MultiDiGraph rationale, node-identity semantics, node-map vs graph-membership distinction, round-half-up quantization, parallel-edge keys, and pickle serialization. 155 tests passed, 2 skipped, 90.29% coverage. Lint/type clean, data-QA PASS (17 checks), judge APPROVE. Commit: 14652a9.
- **DT-006 (add transition edges to graph)**: Complete & approved. `graph.py` updated with `add_transitions()` function adding 63 transition features (42 stairs, 21 elevators) as 126 bidirectional directed edges (84 stairs arcs + 42 elevator arcs) to pathway graph from DT-005, producing `build/graph_with_transitions.pkl` (15,587 nodes, 44,952 directed arcs, 12.6 MB) and `build/graph_with_transitions_stats.json` (cross-level connectivity metadata). 85 transition endpoints protected from contraction via `is_transition_endpoint=True` node attribute. 5 transition-only nodes (endpoints not referenced by pathways) added with complete node-attribute contract derived from transition source rows. 2 express elevators (vertical_order 0→2) handled as single edges. Cross-level connectivity validated per ADR-0005 measured baseline: default routing profile (pathways + stairs + elevators) yields 816 connected components (down from 855 pathway-only baseline, largest component 7,436 nodes, 39 components bridged by transitions with 24 stairs-only contributions); the elevator-only, stairs-excluded profile yields 840 components (largest component 7,060 nodes, 15 components bridged by elevators). Severe pathway-network fragmentation is documented in ADR-0005; routing is limited to within-component origin/destination pairs. This is not wheelchair certification; unverified: door_width, path_width, slope, powered_doors, surface. 212 tests passed, 2 pre-existing skips, 88.19% coverage. Lint/type clean, data-QA PASS (30 checks), judge APPROVE. Commits: 2233feb, 0c89ce4, 031fc5c.

## What changed most recently

- **DT-006 closed & approved:**
  - `packages/wayfinding/src/wayfinding/etl/graph.py`: Added `add_transitions()` function consuming `build/node_map.pkl` from DT-005 to add 63 transition features as 126 bidirectional directed edges to the pathway graph. Transition type mapping: `TRANSITION_TYPE=2` → `mode='stairs'` (42 features, 84 arcs), `TRANSITION_TYPE=4` → `mode='elevator'` (21 features, 42 arcs). Edge keys follow ADR-0004 convention: `TR_{fid}` (forward), `TR_{fid}_R` (reverse). Reverse-arc geometries coordinate-reversed. All 85 unique transition endpoints marked with `is_transition_endpoint=True` node attribute to prevent DT-007 contraction. 5 transition-only nodes (endpoints not referenced by pathway edges) added to graph with complete node-attribute contract (x, y, vertical_order, level_ids, z_min, z_max, z_mean) derived from transition source rows. Express elevators (2 features spanning vertical_order 0→2) handled as single edges without intermediate hops.
  - `build/graph_with_transitions.pkl`: Serialized NetworkX MultiDiGraph (12.6 MB, pickle protocol 5). 15,587 nodes (15,582 from DT-005 + 5 transition-only). 44,952 directed arcs: 44,826 pathway (mode='pathway') + 84 stairs (mode='stairs') + 42 elevator (mode='elevator'). Edge attributes: `{length_3d, mode, level_id_from, level_id_to, vertical_order_from, vertical_order_to, feature_id, geometry: LineString}` for transitions; pathway edges retain DT-005 schema. Shapely LineString objects stored directly.
  - `build/graph_with_transitions_stats.json`: Metadata artifact with transition counts by type (84 stairs arcs, 42 elevator arcs), cross-level connectivity analysis per routing profile, 85 protected transition endpoints, 5 transition-only nodes, 2 express elevators, bridging analysis (39 default-profile components bridged with 24 stairs-only contributions, 15 elevator-profile components bridged), ETL version, ISO 8601 timestamp.
   - Cross-level connectivity validation (ADR-0005): Default routing profile (pathways + stairs + elevators) yields **816 connected components** (down from 855 pathway-only baseline measured in DT-005), largest component 7,436 nodes, 39 components bridged by transitions (24 bridges from stairs-only contributions). The elevator-only, stairs-excluded profile yields **840 connected components**, largest component 7,060 nodes, and 15 components bridged by elevators. Routing is limited to within-component origin/destination pairs. This is not wheelchair certification; unverified: door_width, path_width, slope, powered_doors, surface. Topology-repair disposition is deferred to DT-009.
  - ADR-0005: Documents measured connectivity baseline superseding ADR-0004 §12 global-connectivity assumption. Pathway-only graph has 855 components (severe fragmentation). Transitions reduce to 816 (default) / 840 (elevator-only). Non-regression validation enforced: component count must not increase when adding edges, each transition mode must bridge at least one previously disconnected component (prevents no-op implementation). Global connectivity not required for DT-006; DT-009 owns topology-repair disposition and unit-to-component catalog. Accessible routing explicitly stated as elevator-only (stairs excluded), no wheelchair certification implied, unverified attributes (door_width, path_width, slope, powered_doors, surface) pending facilities survey per system design §4.2 and accessibility-claims instruction.
  - `Makefile`: Added `etl-graph-transitions` target invoking `docker compose run etl python -m wayfinding.etl.run graph-transitions`.
   - `packages/wayfinding/tests/test_dt006_transitions.py`: Comprehensive test suite (212 passed, 2 pre-existing skips, 88.19% coverage). Tests: transition edge count (126 arcs), bidirectionality (all forward/reverse pairs exist), transition type mapping (TRANSITION_TYPE 2→stairs, 4→elevator), mode counts (84 stairs, 42 elevator), edge-attribute schema (8 required keys for transitions), vertical_order provenance from node_map (not Z), reverse-arc geometry (coordinate-reversed), transition endpoint protection (85 nodes marked), transition-only node addition (5 nodes with complete attributes), express elevator handling (2 features spanning vertical_order 0→2 as single edges), cross-level connectivity validation per ADR-0005 (816 default components, 840 elevator-only components, 39/15 bridged components respectively, 24 stairs-only bridge contributions), non-regression (component count ≤ pathway baseline, mode bridging ≥1 component), orphan detection (transition endpoints not in node_map), container-only execution, read-only source.
  - All gates clean: `make test lint type` PASS. `ruff` and `pyright` zero errors. data-QA PASS with 30 validation checks (transition edge count, mode mapping, endpoint protection, transition-only nodes, express elevators, connectivity baselines per ADR-0005, non-regression, bridging analysis, read-only source, no GDB access, accessibility claims validation: elevator-only explicitly stated with no wheelchair certification and unverified attributes listed, pickle protocol 5, stats JSON schema, Makefile target, test suite). `judge` verdict `APPROVE` with no findings. Commits: 2233feb, 0c89ce4, 031fc5c.

## Open questions blocking Phase 1

See [docs/02-system-design.md §12](02-system-design.md#12-open-questions-for-stakeholders) —
closures feed, accessibility survey feasibility, elevator-outage notifications, future building
extracts, room-alias source, elevator-wait/walking-speed constants. None of these block *starting*
Phase 1 ETL, but the accessibility-survey question should be raised with stakeholders before the
accessible-routing UI copy is finalized.

## Next steps (for fresh session)

1. **Plan DT-007 (Contract Degree-2 Chains) with `planner` agent**:
   - Read [docs/plans/PHASE-1-ETL.md](plans/PHASE-1-ETL.md) DT-007 section for scope: collapse runs of degree-2 pathway nodes into single edges carrying full polyline geometry, reducing graph from ~22k edges to ~3k while preserving junctions, transition endpoints (protected via `is_transition_endpoint=True` from DT-006), and unit-connector attachment points (within 0.5 m of unit centroids, reserved for DT-008).
   - Create detailed plan at [docs/plans/DT-007.md](plans/DT-007.md) following established plan template (see DT-003/DT-004/DT-005/DT-006 for reference).
   - Acceptance criteria include: 2500–4000 contracted edges (85–90% reduction), protected nodes never contracted (junctions degree ≥3, transition endpoints, unit-proximity nodes), contracted edge attributes `{length_3d, mode, level_id, geometry: LineString with all intermediate points, original_feature_ids: list}`, shortest-path distance preservation within 0.01 m for 20 random node pairs, mean edge length 8–15 m post-contraction.
2. **Execute DT-007 via `/tdd-feature` workflow**:
   - Invoke `test-writer` to create comprehensive test suite before implementation (RED first).
   - Invoke `implementer` to extend `packages/wayfinding/src/wayfinding/etl/graph.py` with `contract_degree2_chains(graph)` function, consuming `build/graph_with_transitions.pkl` from DT-006.
   - Output `build/graph_contracted.pkl` (production routing graph with contracted edges, no unit connectors yet).
3. **Judge review & doc-writer**:
   - Run `judge` agent against plan, DOD, and spatial-invariants (transition endpoints must survive contraction, Z coordinates excluded from node identity).
   - Update CHANGELOG, HANDOFF, and advance to DT-008 (Connect Unit Centroids to Graph).
