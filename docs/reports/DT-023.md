# DT-023 — Three-building overview increment

## Building/dropdown synchronization — 2026-09-14

The bounded synchronization increment passed fresh full Docker and browser/API
verification. The full campus-routing ticket remains incomplete. ADR-0016 now
defines one synchronous building/floor selection: retain a recorded global order,
otherwise choose recorded order 0, otherwise the first deterministically ordered
level. Overview-only buildings retain their facility with an empty, disabled Floor
control. Building inspection preserves endpoints/profile and does not load a scene
or request a route. Hidden-floor cleanup on Clear or route start no longer restores
old dropdowns; explicit floor/step navigation restores normal controls.

RED evidence before the production fix:

- Actual client: `build/dt023-sync/client-red.log`, Docker pytest exit **1**,
  Building remained `AQ` instead of the selected overview-only `SH` fixture.
- Real Chromium through published port 18117:
  `build/takeover-browser/dt023-campus-red-sync/results.json`, exit **1**,
  **14:08:32.254–14:08:35.973 UTC**. The new exact Building-control assertion failed.
- Focused final client checks: **4 passed**, exit **0**,
  `build/dt023-sync/focused.log`; container
  `sfudt-dt023-sync-focused-artifact-run-28d51b3a077c`.

The actual-JavaScript fixture covers shared-order retention, both fallback rules,
no-level selection/recovery, hidden-floor Clear/route-start cleanup, visible-floor
restoration, preserved endpoints/profile and late scene/route rejection. The shared
Chromium campus test exercises pointer, Enter and Space selection, AQ-only order
to each of ECC and Strand, shared nonzero AQ/Strand order, no implicit scene/route
request, and dropdown/context retention after a delayed scene. It records the
synchronized controls at desktop, mobile and 320 px. Overview-only and first-level
fallback use synthetic fixtures because all three live buildings record order 0.

| Fresh browser run | UTC start / finish | Requests | Exit |
| --- | --- | --- | --- |
| `dt023-sync-candidate-final2` | 14:12:12.021 / 14:22:10.476 | 165 GET / 23 POST | 0 |
| `dt023-sync-accepted-final2` | 14:12:12.404 / 14:22:08.760 | 228 GET / 39 POST | 0 |

Both passed all campus checks at 1440×1000, 390×844 and 320×844. Candidate E2E
exercised all 11 recorded fixtures at desktop/mobile plus a 320px route; accepted
E2E exercised four canonical fixtures at desktop/mobile plus Clear, Swap, assistant,
availability, delayed scene/step and 320px regressions. No browser assertion was
skipped. Results and synchronized screenshots are in the named directories under
`build/takeover-browser/`. Independent large-model code and desktop/mobile/320px
visual review found no material blocker. At 320px, native Floor selection clips
trailing text in the inherited globally aligned label; the control remains usable
and the page does not overflow.

Full Docker regression passed, exit **0**: **617 passed, 16 skipped, 86.25% coverage**
in **935.34 seconds**. Container
`sfudt-dt023-sync-gates-artifact-run-c9469f3d1f3b`, ID
`240527ad54f3d16629d093cdd4bf2d1912efc885ba24134bd4b91732dc957adb`,
started **14:10:36.666 UTC**, used the local immutable image ID recorded below.
Coverage was written at **14:26:10.400 UTC**; the suite then exited successfully.
The 16 skips are the established 12 source-GDB checks, two unavailable Docker-CLI
checks inside the offline image, one unavailable accepted-artifact profile-isolated
fixture, and one source-QA capability check. Source reconstruction is not required
for this client-only increment; no source or artifact changed.

Ruff, Pyright, artifact QA (**23 passed, 1 skipped, 609 deselected**), launcher,
no-host-Python and rebuild-launcher gates all exited **0**. Logs and isolated coverage
are under `build/dt023-sync/gates/`. The full test command was:

```powershell
docker compose -p sfudt-dt023-sync-gates -f infra/docker-compose.yml -f infra/docker-compose.local.yml run --rm artifact sh -c 'mkdir -p /workspace/build/dt023-sync/gates; env PYTHONPATH=/workspace/packages/wayfinding/src COVERAGE_FILE=/workspace/build/dt023-sync/gates/.coverage pytest packages/wayfinding/tests --basetemp=/tmp/dt023-sync-gates-pytest'
```

The lead independently observed Docker container exit **0** with `docker wait`.
Full stdout/stderr was streamed from Docker CLI into a separate offline Docker
`cat` process writing `gates/full.log`; no host evidence processing was used.
The temporary `sfudt-dt023-sync-accepted` demo/network were removed using their exact
Compose project after successful E2E (exit **0**). The dedicated preview and unrelated
stacks remain running. Final diff whitespace check passed.

Final independent `gpt-6-astra` judge review approved this bounded synchronization
increment after inspecting the integrated implementation, documentation, gate logs
and all three viewport screenshots. No material blocker remained. This approval
does not extend to full-campus routing, publication, physical LAN or artifact promotion.

Auxiliary invocations used the same Compose prefix as the full test command and
`artifact sh -c 'mkdir -p /workspace/build/dt023-sync/gates; <command> >
/workspace/build/dt023-sync/gates/<log> 2>&1; code=$?; cat
/workspace/build/dt023-sync/gates/<log>; exit $code'`. Exact inner commands and
container-name suffixes (`sfudt-dt023-sync-gates-artifact-run-…`) are:

| Inner command | Log | Container suffix | Exit |
| --- | --- | --- | --- |
| `env PYTHONPATH=/workspace/packages/wayfinding/src ruff check packages/wayfinding` | `lint.log` | `d19fa90553c9` | 0 |
| `env PYTHONPATH=/workspace/packages/wayfinding/src pyright packages/wayfinding/src` | `type.log` | `9d96a2db302d` | 0 |
| `env PYTHONPATH=/workspace/packages/wayfinding/src pytest packages/wayfinding/tests -m dataqa --strict-markers --no-cov --basetemp=/tmp/dt023-sync-gates-dataqa2` | `dataqa.log` | `3cc067e1a3ee` | 0 |
| `sh tests/gate/test_launch_stack.sh` | `launcher.log` | `a99276c93fa8` | 0 |
| `sh tests/gate/test_no_host_python.sh` | `no-host-python.log` | `20f04939d806` | 0 |
| `bash tests/gate/test_rebuild_launcher.sh` | `rebuild-launcher.log` | `30d0e8315516` | 0 |

An earlier auxiliary data-QA invocation omitted `PYTHONPATH` and failed collection
with `ModuleNotFoundError`. The corrected invocation above passed; no code or test
was changed to resolve that command-environment error.

Final browser commands (production and shared harness frozen before these runs):

```powershell
docker run --rm --name sfudt-dt023-sync-host-final2 --init --shm-size 1g -w /workspace --add-host host.docker.internal:host-gateway -v C:/repos/sfudt/ghcp/tests/browser:/workspace/tests/browser:ro -v C:/repos/sfudt/ghcp/build/experiments/evaluations:/workspace/build/experiments/evaluations:ro -v C:/repos/sfudt/ghcp/build/takeover-browser:/workspace/build/takeover-browser:rw -e WAYFINDING_CANDIDATE_REPORT=/workspace/build/experiments/evaluations/dt022-final3.json -e WAYFINDING_CANDIDATE_RUN=dt022-exact-final3 -e WAYFINDING_BROWSER_RUN_LABEL=dt023-sync-candidate-final2 mcr.microsoft.com/playwright@sha256:7dbbf924428aad5c87a5a3a5bc38f23e110cb1f5427fbbc7dbc3231014a4b0db node /workspace/tests/browser/host-preview.cjs

$env:WAYFINDING_BROWSER_BUILD_IMAGE='sha256:0b67be1d851168d326c422762b369d1ce129a9ed3c20429d5014c98ed10a4947'
docker compose -p sfudt-dt023-sync-accepted -f infra/docker-compose.browser.yml up -d demo
$env:WAYFINDING_BROWSER_RUN_LABEL='dt023-sync-accepted-final2'
docker compose -p sfudt-dt023-sync-accepted -f infra/docker-compose.browser.yml run --rm --no-deps browser
```

Candidate target remains `sfudt-dt023-review`, container
`cca8c5abe3e8a3c9a7cb6046d78a85f052a7679bc44b168c219690ce1b55a49b`.
Its static client is mounted from this checkout; no backend/artifact restart was
required. The fresh accepted regression demo is
`9c9beb7340f4fd47b738538b4af8731cb7b3fd779b9d3a9bca204de96ae9ebbc`,
started **14:10:42.223 UTC**. Both use the local build image above.
Candidate browser: `b83ddc79e64edb8a2c05d3ec7a17ff8efec67b1d630cdd4be97823164c0b00f4`,
started **14:12:11.253 UTC**. Accepted browser:
`2321c82156d75aa56ab0fbe69e5132e0a59a47cabdb7cba8d21f7ea7a4e84c69`,
started **14:12:11.694 UTC**. Both use the pinned Chromium image in the command.

Earlier exploratory `dt023-red-baseline` and `dt023-sync-candidate-final` runs were
stopped before completion (Docker stop exit 0); neither is acceptance evidence.
The latter preceded the final browser coverage additions. No production code changed
after the focused pass. Accepted/source artifacts and topology remain unchanged;
the unpromoted DT-022 candidate is only the preview target. Published-image, physical
LAN and full-campus GIS gates remain open. No commit, push or promotion is implied.

Docker-generated file identity: `build/dt023-sync/runnable-state.json`, capture
exit **0** at **14:14:22.449 UTC**. This fingerprints the five named runnable/test
files, not the entire dirty checkout. Client SHA-256:
`0996e2047c5ec5909cae41466cc1c1f994d4592792b38f924711dc6eadfe1e9e`.
The recorded manifest digest is
`54fe2e3e2d8095ea3450863dde7dd290882bd923ef31f2762274a0f669559a02`.

| Artifact | Candidate preview SHA-256 | Accepted regression SHA-256 |
| --- | --- | --- |
| GeoPackage | `baf80690197f1fb00e2eca58eb3c0835ae9f9b65667da843a683679d5f240eb9` | `f3d3d97e7749d1606739a078195eebc736279b780cd62076e994908652e71f65` |
| Contracted graph | `93c2ff2994e6e564adecae3506175f2c0047948e1d84e44045d2c70267071d46` | `ed020258ae8a39410db3f51c5e709b5bb4f8a18755c426c853635b24705d789d` |
| Graph sidecar | `e70b32760caded0f47b461530f335a41d6cee018bb34ad1dbdbc1f56ba77173a` | `2a88700a7c829c0146303f258cbbc82e03541c9e8a8ab4b5731c29a300c199e1` |

## Historical planning update — 2026-09-14 (superseded above)

Added the next-session building-click/dropdown synchronization requirement to
`docs/plans/DT-023.md` and the top of `docs/HANDOFF.md`, with a planned CHANGELOG
entry. The contract covers facility/floor consistency, deterministic valid-floor
fallback, no-floor buildings, keyboard/list parity, stale responses and preserved
route state. Independent documentation review found no material issues.
**At that time the feature was planned, not implemented.** Application code and artifacts were
unchanged in this planning session.

Fresh Docker host-port browser/API E2E of the existing candidate passed, exit **0**:
**2026-09-14 13:43:44.566–13:50:52.916 UTC**, **162 GET / 23 POST**, desktop
1440×1000, mobile 390×844 and 320×844, Chromium 130.0.6723.31. Evidence:
`build/takeover-browser/dt023-planning-20260914/`. The existing fixtures do not test
or certify the planned dropdown behavior; that requires next-session RED and new
acceptance coverage. Earlier full Python gate results remain historical, not rerun
for this documentation-only update.

An initial `docker compose -p sfudt-dt023-planning-browser -f
infra/docker-compose.candidate.yml run --rm --no-deps browser node
/workspace/tests/browser/host-preview.cjs` attempt exited **1**: the internal test
network could not resolve `host.docker.internal` (`EAI_AGAIN`). The successful
host-path run used the explicit gateway mapping below, with no service changes:

```powershell
docker run --rm --name sfudt-dt023-planning-host-browser --init --shm-size 1g -w /workspace --add-host host.docker.internal:host-gateway -v C:/repos/sfudt/ghcp/tests/browser:/workspace/tests/browser:ro -v C:/repos/sfudt/ghcp/build/experiments/evaluations:/workspace/build/experiments/evaluations:ro -v C:/repos/sfudt/ghcp/build/takeover-browser:/workspace/build/takeover-browser:rw -e WAYFINDING_CANDIDATE_REPORT=/workspace/build/experiments/evaluations/dt022-final3.json -e WAYFINDING_CANDIDATE_RUN=dt022-exact-final3 -e WAYFINDING_BROWSER_RUN_LABEL=dt023-planning-20260914 mcr.microsoft.com/playwright@sha256:7dbbf924428aad5c87a5a3a5bc38f23e110cb1f5427fbbc7dbc3231014a4b0db node /workspace/tests/browser/host-preview.cjs
```

## Published preview repair — 2026-09-12

The user reported that `http://127.0.0.1:18117/` did not load. The earlier URL
availability claim was incorrect: browser E2E used the demo's own Docker network
namespace and therefore bypassed Windows port publication.

RED diagnostic: the healthy container's `HostConfig.PortBindings` requested
`127.0.0.1:18117`, but `NetworkSettings.Ports` showed `{"8080/tcp":[]}` and
`docker ps` showed only `8080/tcp`. The container was attached only to the internal
candidate network. The configured binding alone was not evidence of publication.

Repair (all commands exited 0):

```powershell
docker network connect bridge sfudt-dt023-review
docker port sfudt-dt023-review
docker restart sfudt-dt023-review
docker port sfudt-dt023-review
```

The port now reports `8080/tcp -> 127.0.0.1:18117` and survived restart at
**2026-09-12 03:56:59.958 UTC**. Candidate/artifact mounts, application code and
loopback-only binding are unchanged. The bridge attachment persists for this
container; repeat it if recreating the preview from the original Compose command.
The offline test network and ETL network configuration are unchanged.

Docker HTTP probes through `host.docker.internal:18117` (Host header
`127.0.0.1:18117`) returned **200** for health, `/`, `/app.js` and `/demo/v1/campus`,
including after restart. Health identifies the same unpromoted candidate hashes.

New checked-in `tests/browser/host-preview.cjs` forces browser/API traffic through
the Windows-published port. Inside the browser container, a fixed local proxy at
`127.0.0.1:18118` forwards to `host.docker.internal:18117`, using the allowed target
Host header. It then runs the complete existing candidate browser suite. The test
container does not join the demo's network namespace, so an unpublished port fails.
This tests the host publication path, not physical second-device LAN acceptance.

Host-path browser E2E **passed, exit 0**, from **03:57:46.220 to 04:04:19.424 UTC**
on 2026-09-12: 11 recorded route fixtures at each desktop/mobile size, plus campus
and route checks at 320 px, **162 GET and 23 POST requests**, no browser skips or
errors. Evidence: `build/takeover-browser/dt023-host-port-final/`. No application/
source change requires reconstruction or changes the earlier full regression results.
Browser container: `65b3bcdc93d6ce2b3785dc0851673c0f3ded01094246e49792c268bd9f2db2f4`,
started **03:57:45.773 UTC**, using the same pinned Chromium image as the earlier
runs. Independent review confirmed the fixed proxy destination and effective
loopback publication, with no material code or network-scope finding.

```powershell
docker run --rm --name sfudt-dt023-host-browser --init --shm-size 1g -w /workspace -v C:/repos/sfudt/ghcp/tests/browser:/workspace/tests/browser:ro -v C:/repos/sfudt/ghcp/build/experiments/evaluations:/workspace/build/experiments/evaluations:ro -v C:/repos/sfudt/ghcp/build/takeover-browser:/workspace/build/takeover-browser:rw -e WAYFINDING_CANDIDATE_REPORT=/workspace/build/experiments/evaluations/dt022-final3.json -e WAYFINDING_CANDIDATE_RUN=dt022-exact-final3 -e WAYFINDING_BROWSER_RUN_LABEL=dt023-host-port-final mcr.microsoft.com/playwright@sha256:7dbbf924428aad5c87a5a3a5bc38f23e110cb1f5427fbbc7dbc3231014a4b0db node /workspace/tests/browser/host-preview.cjs
```

Status: bounded overview/audit increment verified locally. DT-023 as a whole is incomplete.

Branch: `feature/dt-023-campus-routing`. Existing HANDOFF, ROADMAP, GIS request
and DT-023 planning changes were preserved when branching from local `main`.
No remote synchronization, commit, push, publication or artifact promotion is claimed.

## Scope

ADR-0016 authorizes a runtime overview derived from the same trusted GeoPackage:
three source facility footprints, building search, recorded floor selection and
explicit partial coverage. Ordered directed room-pair measurements are a separate
Docker audit. No new topology, outdoor edge, portal, building route destination or
accessibility claim is introduced. Stage 0 reconciliation and Stages 2–4 remain open.

## RED evidence

- Backend: `build/dt023-gates/backend/red.log`, three failed tests before the API
  implementation, exit 1.
- Client: `build/dt023-gates/client/red.log`, missing campus behavior, exit 1.
- Real candidate browser: `build/takeover-browser/dt023-campus-red/results.json`,
  exit 1 because the existing application has no initial campus view. The target
  was the existing isolated DT-022 candidate; no stack was replaced for RED.

## Final verification

Full Docker regression passed **617 tests, 16 expected skips, 86.25% coverage**,
exit 0, in **756.30 seconds**. Ruff, Pyright, artifact QA (**23 passed, 1 skipped**),
launcher and no-host-execution checks passed. Logs/JUnit are in
`build/dt023-gates/final/`. Both fresh browser/API runs passed against
the final production code and corrected browser fixture:

| Run | Started / finished (UTC, 2026-09-12) | Exercised routes | Requests | Exit |
| --- | --- | --- | --- | --- |
| `dt023-candidate-final` | 02:57:12.764 / 03:04:35.293 | 11 recorded fixtures each at desktop and mobile, plus 320px route | 162 GET / 23 POST | 0 |
| `dt023-accepted-final2` | 02:57:13.250 / 03:04:53.557 | Four canonical fixtures each at desktop and mobile, camera/choice/failure/stale regressions, plus 320px route | 225 GET / 39 POST | 0 |

Both runs exercise the campus view at 1440×1000, 390×844 and 320×844: deterministic
API/source identity, all footprints inside the SVG viewport, building search,
keyboard footprint/list selection, real floor choices, endpoint preservation,
zoom/reset and delayed floor response rejection. No browser assertions were skipped.
Results and screenshots are under `build/takeover-browser/<run>/`.
The candidate run compares returned route/guidance/availability against the recorded
DT-022 candidate fixtures; it does not substitute accepted fixtures for that candidate.

Independent large-model visual review passed desktop, mobile and 320px presentation.
Mobile campus screenshots show only the map's upper portion because of normal page
scrolling. Full footprint containment is established by the executable screen-space
SVG assertions, not by those screenshots alone. The temporary accepted regression
container/network was removed after verification; the dedicated candidate preview remains.

The available development image is
`sha256:0b67be1d851168d326c422762b369d1ce129a9ed3c20429d5014c98ed10a4947`.
Local-image execution does not certify the published GHCR image. Browser execution
uses `mcr.microsoft.com/playwright@sha256:7dbbf924428aad5c87a5a3a5bc38f23e110cb1f5427fbbc7dbc3231014a4b0db`.

## Exercised stacks and commands

The candidate uses existing isolated `dt022-exact-final3/derived` artifacts. This
delivery changes their read-only presentation, not the importer or source-to-candidate
pipeline. No new reconstruction or promotion is claimed. Source audit E2E was rerun
after Python application freeze; see [DT-023-GIS](DT-023-GIS.md) for its exact Docker
command, input hashes, directed counts and source exclusions.

The candidate preview is separately exposed at **http://127.0.0.1:18117/**.
The previously running port-18087 and port-18107 stacks were not restarted. Their
mounted static files may reflect this checkout; reload the dedicated port-18117
stack to inspect the matching backend and client together.

```powershell
$env:WAYFINDING_BROWSER_BUILD_IMAGE='sha256:0b67be1d851168d326c422762b369d1ce129a9ed3c20429d5014c98ed10a4947'
$env:WAYFINDING_CANDIDATE_RUN='dt022-exact-final3'
docker compose -p sfudt-dt023-review -f infra/docker-compose.candidate.yml run -d --no-deps --name sfudt-dt023-review --publish 127.0.0.1:18117:8080 demo
docker network connect bridge sfudt-dt023-review
docker compose -p sfudt-dt023-accepted -f infra/docker-compose.browser.yml run -d --no-deps --name sfudt-dt023-accepted demo
```

Both stacks were restarted after final production code was assembled (exits 0),
at **02:56:59 UTC** and **02:57:01 UTC**, respectively, on 2026-09-12.
Candidate container ID: `cca8c5abe3e8a3c9a7cb6046d78a85f052a7679bc44b168c219690ce1b55a49b`.
Accepted regression container ID: `1082735b19ba35a3d5befad3774f85fd9bd5ce338a39b18349d29844a4f5907c`.

Browser invocation, from the repository root:

```powershell
$browserImage='mcr.microsoft.com/playwright@sha256:7dbbf924428aad5c87a5a3a5bc38f23e110cb1f5427fbbc7dbc3231014a4b0db'
$common=@('run','--rm','--init','--shm-size','1g','-w','/workspace',
  '-v','C:/repos/sfudt/ghcp/tests/browser:/workspace/tests/browser:ro',
  '-v','C:/repos/sfudt/ghcp/build/takeover-browser:/workspace/build/takeover-browser:rw',
  '-e','WAYFINDING_BROWSER_BASE_URL=http://127.0.0.1:8080')
docker @common --name sfudt-dt023-candidate-browser --network container:sfudt-dt023-review -v C:/repos/sfudt/ghcp/build/experiments/evaluations:/workspace/build/experiments/evaluations:ro -e WAYFINDING_CANDIDATE_REPORT=/workspace/build/experiments/evaluations/dt022-final3.json -e WAYFINDING_CANDIDATE_RUN=dt022-exact-final3 -e WAYFINDING_BROWSER_RUN_LABEL=dt023-candidate-final $browserImage node /workspace/tests/browser/candidate.cjs
docker @common --name sfudt-dt023-accepted-browser --network container:sfudt-dt023-accepted -v C:/repos/sfudt/ghcp/docs/reports/DT-015-demo-routes.json:/workspace/docs/reports/DT-015-demo-routes.json:ro -v C:/repos/sfudt/ghcp/build/wayfinding.gpkg:/workspace/build/wayfinding.gpkg:ro -v C:/repos/sfudt/ghcp/build/graph_contracted.pkl:/workspace/build/graph_contracted.pkl:ro -v C:/repos/sfudt/ghcp/build/graph_contracted_stats.json:/workspace/build/graph_contracted_stats.json:ro -e WAYFINDING_BROWSER_RUN_LABEL=dt023-accepted-final2 $browserImage node /workspace/tests/browser/demo.cjs
```

Candidate browser ID: `ea7547b5ea88f8a11424038dacb35587ab26a23f85f02056337dd4e1eb879d3f`.
Accepted browser ID: `b7c75da53f5e07441243670ef4488c106812dabc3dd24f6e8d98e4d0da4dadef`.
Both use the pinned Chromium image above and each shares only its own demo's
network namespace. No package installation or external network is involved.

Earlier `dt023-campus-integration` and `dt023-accepted-final` browser attempts were
intentionally stopped; both attached Docker invocations returned exit 1. They are
not acceptance evidence. The repeated
delayed-scene fixture needed to reset its release callback between viewports;
the final invocations above contain that correction. The production code was unchanged
by this test-harness correction.

Cleanup: Compose `down` returned 0 but could not remove the one-off accepted demo's
network while it remained attached. `docker rm -f sfudt-dt023-accepted` followed by
`docker network rm sfudt-dt023-accepted_browser` each returned 0 and removed only
this temporary regression stack.

## Review and limits

Full gate command (all project execution inside the container):

```powershell
docker compose -p sfudt-dt023-final -f infra/docker-compose.yml -f infra/docker-compose.local.yml run --rm artifact sh -c 'mkdir -p build/dt023-gates/final; export PYTHONPATH=/workspace/packages/wayfinding/src; export COVERAGE_FILE=/workspace/build/dt023-gates/final/.coverage; pytest packages/wayfinding/tests --basetemp /tmp/dt023-final --cache-clear --junitxml=build/dt023-gates/final/full.xml --cov=wayfinding --cov-report=term-missing --cov-fail-under=85 > build/dt023-gates/final/full.log 2>&1; status=$?; cat build/dt023-gates/final/full.log; exit $status'
```

Full-suite container: `sfudt-dt023-final-artifact-run-40e5ada6f589`; exit 0.
The suite finished at approximately **03:07:33 UTC on 2026-09-12** (JUnit/log write
time). The 16 skips are the established 12 source-GDB checks, two Docker-CLI
availability checks inside the offline test image, one unavailable accepted-data
profile-isolated fixture, and one source-QA capability check. The separate final
read-only GIS audit exercised the revised-source slice; it does not turn skipped
source importer tests into passes.

Auxiliary commands used fresh local-image artifact containers: `ruff check
packages/wayfinding tools/capture_takeover_evidence.py tools/hooks/check_read_only_boundary.py
tools/dt023_gis_audit.py`, `pyright packages/wayfinding/src`, and `pytest
packages/wayfinding/tests -m dataqa --strict-markers --no-cov` (all exit 0).
The no-host Python gate passed; its output is `nohost.log`. The lead also reran
`sh tests/gate/test_no_host_python.sh` directly after restoring canonical LF endings,
exit 0, `nohost-direct.log`, project `sfudt-dt023-direct-boundary`.

Launcher verification initially failed: one invocation lost the script's directory
context; a later direct invocation encountered Windows CRLF working copies of LF
shell scripts (`launcher.log`). The worker's temporary normalized-copy pass
(`launcher-final.log`) was not accepted as the direct gate. The lead restored the
six clean launcher/test shell working copies to the existing Git index's LF bytes.
`git ls-files --eol` then showed `i/lf w/lf`; the files have no Git content diff.
This is a checkout correction, not a launcher implementation or application change.

The actual checked-in paths subsequently passed directly, without temporary copies:

```powershell
docker compose -p sfudt-dt023-direct-launchers -f infra/docker-compose.yml -f infra/docker-compose.local.yml run --rm artifact sh -c 'sh tests/gate/test_launch_stack.sh > build/dt023-gates/final/launcher-direct.log 2>&1; status=$?; cat build/dt023-gates/final/launcher-direct.log; exit $status'
docker compose -p sfudt-dt023-direct-rebuild -f infra/docker-compose.yml -f infra/docker-compose.local.yml run --rm artifact sh -c 'bash tests/gate/test_rebuild_launcher.sh > build/dt023-gates/final/rebuild-launcher-direct.log 2>&1; status=$?; cat build/dt023-gates/final/rebuild-launcher-direct.log; exit $status'
```

Both exited **0**. Containers: `sfudt-dt023-direct-launchers-artifact-run-2d78206931a7`
and `sfudt-dt023-direct-rebuild-artifact-run-0f5cba557229`. The final browser runs
used the same unchanged application, Compose configuration and artifacts; restoring
canonical shell line endings did not alter their runnable application state.

Independent large-model review found no material backend or GIS counting blocker.
Separate client review found no blocker in state handling or desktop layout.
The synthetic actual-JavaScript fixture checks delayed route cancellation and
an overview-only building; real Chromium checks delayed scene cancellation.
Only the three real buildings are present in browser data. Outdoor/portal and
accessibility scenarios remain unimplemented and unrun because those stages are gated.
Physical LAN acceptance and published-image verification remain separate open items.

Final runnable-file and accepted-artifact identities are captured by
`tools/capture-takeover-evidence.ps1 -LocalImage` in `build/takeover-state.json`
(exit 0). Hash generation runs inside Docker. That launcher reported unrelated
pre-existing Compose orphans; none was removed or restarted.
