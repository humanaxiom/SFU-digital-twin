# Reports

Generated build/test/data-QA reports that are worth keeping (e.g. an ETL `build-report.json`
summary, a data-QA connectivity report) land here once Phase 1+ produces them. Nothing here yet —
raw tool output for the Phase 0 data findings lives in [docs/generated/](../generated) instead.
