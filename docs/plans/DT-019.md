# DT-019 — Actionable rebuild image diagnostics

Status: implemented; Docker checks and fresh PowerShell-to-Docker build passed.
Evidence: `docs/reports/DT-019.md`.

The user's PowerShell rebuild failed before extraction because the pinned published
image could not be inspected locally. The existing local build image is available.
Keep explicit `-LocalImage` / `--local-image` selection; do not silently fall back,
pull, change the default pin, or replace accepted artifacts.

1. Demonstrate missing-image diagnostics failing a Docker launcher regression.
2. Give actionable local-image instructions while preserving Docker's actual error;
   distinguish inspection failure from malformed successful image IDs.
3. Document the exact PowerShell command and its legacy-source scope.
4. Run Docker launcher checks and a fresh complete source-to-candidate rebuild via
   the real PowerShell launcher with an unused run ID. Record stage completion,
   image/source/artifact identities and exits; inspect existing-artifact preservation.
5. Obtain independent large-model review and update the handoff with results.

Only launcher diagnostics and documentation change. ETL, graph semantics, revised
source admission and application behavior remain separate. All project execution
stays in Docker; PowerShell on the host only launches Docker and forwards results.
